# Test Report

> Hand-authored (not generated by the test reconciler). The TestAgent cannot
> validate this action's happy paths: softprops/action-gh-release creates a real
> GitHub release, which 404s in the hermetic act harness (no writable target
> repo). These two negative tests exercise the input-validation guards that fail
> before any API call, so they run deterministically and still confirm the
> hardened artifact loads and parses inputs.

**Test Policy SHA:** `843adf9e4b8f85d0c08b27b9d0b09dd094b54702`
**Test Agent Version:** `1`
