name: Test basic release inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/tags/v1.0.0
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          tag_name: v1.0.0-test
          name: "Test Release v1.0.0"
          body: "This is a test release body"
          make_latest: "false"
          token: ${{ github.token }}
      - name: Assert inputs were accepted
        run: |
          echo "Action outcome: ${{ steps.action.outcome }}"
          echo "tag_name, name, body, make_latest inputs were passed successfully"
          echo "PASS: inputs accepted"
